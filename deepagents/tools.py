def search_web(query: str) -> str:
    """Simula uma busca na web e retorna um resultado ficticio para a query."""
    return (
        f"Resultado simulado para a busca '{query}': "
        "deep agents sao agentes construidos sobre o LangGraph, com suporte "
        "a planejamento (todo list), sub-agentes e um sistema de arquivos virtual."
    )
